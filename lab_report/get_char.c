
// still incomplete program and needed to made some changes


# include<stdio.h>

# include<conio.h>
int main()
{
    char ch;
    printf("enter a character: ");
    ch=getchar();
    printf("entered character is: ");
    putchar(ch);

    printf("\ncase 1,enter a character: ");
    ch=getche();
    printf("\nentered character is:");
    putch(ch);    

    // char str[45];
    // printf("\nenter the string: ");
    // gets(str);
    // printf("\nentered string is:");
    // puts(str); 

return 0;
}